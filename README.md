# Circle CI Docker Builder

### A utility Docker image based on `docker:stable-git` to assist in Circle CI builds with Python.

This image has make, git, docker, python3, and docker-compose installed
